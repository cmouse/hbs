#include "hbs.h"

void misc_command_meminfo(NICK *nick, CHANNEL *channel, const char *cmd,
			 const char **args, int argc);
void misc_command_bwinfo(NICK *nick, CHANNEL *channel, const char *cmd,
			 const char **args, int argc);
void misc_command_weed(NICK *nick, CHANNEL *channel, const char *cmd,
                         const char **args, int argc);
void misc_command_params(NICK *nick, CHANNEL *channel, const char *cmd,
			 const char **args, int argc);
void misc_command_smack(NICK *nick, CHANNEL *channel, const char *cmd,
			const char **args, int argc);
void misc_command_auth(NICK *nick, CHANNEL *channel, const char *cmd,
		       const char **args, int argc);
void misc_command_google(NICK *nick, CHANNEL *channel, const char *cmd,
			 const char **args, int argc);
void misc_command_lamercontrol(NICK *nick, CHANNEL *channel, const char *cmd,
			       const char **args, int argc);
void misc_command_calc(NICK *nick, CHANNEL *channel, const char *cmd,
		       const char **args, int argc);
void misc_command_seen(NICK *nick, CHANNEL *channel, const char *cmd,
                       const char **args, int argc);

